import { Card, CardHeader, CardTitle } from '@/components/ui/card'

const List = ({}) => {
	return (
		<Card>
			<CardHeader>
				<CardTitle>Товары</CardTitle>
			</CardHeader>
		</Card>
	)
}

export default List
